{
  "name": "babel-plugin-animated-block",
  "version": "1.0.0",
  "main": "built.js",
  "license": "MIT",
  "scripts": {
    "prepublish": "rm -rf plugin && mkdir plugin && babel index.js -o plugin/index.js && babel index.js -o built.js",
    "test": "jest"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.18.0",
    "babel-core": "^6.24.1",
    "babel-jest": "^20.0.3",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",
    "babel-preset-es2015": "^6.18.0",
    "jest": "^20.0.4"
  },
  "dependencies": {
    "babel-plugin-syntax-decorators": "^6.13.0"
  }
}
